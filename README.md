# MI-MIDI-Demo

Static showcase demo for local MIDI steering samples.

## Run

Open `index.html` directly in a browser, or serve the repo root:

```bash
python -m http.server 8000
```

Then visit `http://localhost:8000`.